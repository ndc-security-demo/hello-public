name: Linting etc
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  pull_request:
jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - name: hi
        run: |
          ssh test@test.com