name: Deploy to appropriate environment
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
    steps:
      - name: Do something
        env:
          SECRET: ${{ secrets.ENV_SECRET }}
        run: |
          echo "Place holder for doing a deploy or something with the secret"